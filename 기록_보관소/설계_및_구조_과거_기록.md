# 🏗️ 4. 설계 및 구조 (시스템/웹 구현)

_이 문서는 프로젝트의 기술 스택, 폴더 구조, 컴포넌트 설계 등 실제 개발 구현을 위한 **'시스템 설계 도면'**입니다._
_원칙: 누적 기록제 (Strict Append-only) - 설계의 진화 과정을 하단에 지속적으로 누적하며, 절대 축약하지 않는다._

---

## 📅 [2026-03-06 01:00:00] 초기 기반 설계

### **🛠️ 1. 기술 스택 (Tech Stack)**

- **Frontend**: React 19, Vite 6, TypeScript, Vanilla CSS.
- **Backend/Collector**: Python 3.14 (.venv), requests, BeautifulSoup.
- **Data Storage**: JSON 기반 파일 스토리지 (`store/raw`, `store/refined`).

### **📂 2. 프로젝트 폴더 구조 (System Directory Tree)**

```text
my-project/
├── .venv/                      # Python 가상 환경
├── 프로젝트_기록/              # 핵심 설계 및 로그 문서
├── data/                       # [예정] 데이터 수집 및 가공 엔진
└── web/                        # React 프론트엔드 (Vite 6)
    ├── public/                 # 정적 자산
    └── src/
        ├── components/
        │   └── organisms/      # Header.tsx (단계적 레이어 GNB)
        ├── constants/
        │   └── categories/     # 도메인별 모듈화 (performance.ts 등)
        ├── pages/
        │   └── Home.tsx        # 여기어때 스타일 큐레이션 홈
        ├── types/              # culture.ts (L1-L6 인터페이스 정의)
        ├── App.tsx             # 루트 컴포넌트 및 기본 스타일
        ├── index.css           # 글로벌 스타일 (데스크탑 와이드 최적화)
        └── main.tsx            # 엔트리 포인트
```

---

## 📅 [2026-03-07 12:40:00] [누적] USB Portable Mode 대응 및 전체 구조 확정

### **🛠️ 1. [최신화] 인프라 (Infrastructure)**

- **USB Portable Mode**: 드라이브 문자 변경 시에도 경로를 자동 추적하는 `g.bat` & `g.ps1` 하이브리드 부트스트랩 (V7.6).

### **📂 2. [완전판] 상세 프로젝트 폴더 구조 (Literal Directory Tree)**

```text
H:\PROJECT-ALL\my-project
|   .gitignore
|   README.md
|   g.ps1
|   g.bat
|
+---.vscode
|       settings.json
|
+---AI_CORE
|       IDENTITY.md
|
+---프로젝트_기록
|       1.목표_및_전략.md
|       2.참고_사이트_분석.md
|       3.프로젝트_상세_구조도.md
|       4.설계_및_구조.md
|       5.작업_현황_및_로그.md
|       6.트러블슈팅_및_교훈.md
|
+---data
|   +---agents
|   |       __init__.py
|   |       consent.txt
|   |       debate.txt
|   |       debater.py
|   |       guardian.py
|   |       inspector.py
|   |       live_chat.md
|   |       plan.txt
|   |       pre_checker.py
|   |       roles.json
|   |       rules.json
|   |       task.txt
|   |       watcher.py
|   |
|   +---backups
|   \---logs
|
+---web
    |   package.json
    |   index.html
    |   vite.config.ts
    |   tsconfig.json
    |   tsconfig.node.json
    |   package-lock.json
    |
    +---src
    |   |   main.tsx
    |   |   index.css
    |   |   App.tsx
    |   |   App.css
    |   |
    |   +---components
    |   |   +---atoms
    |   |   +---molecules
    |   |   +---organisms
    |   |   |       Header.tsx
    |   |   |       Header.css
    |   |   |
    |   |   \---templates
    |   +---constants
    |   |   \---categories
    |   |           performance.ts
    |   |           cinema.ts
    |   |           exhibition.ts
    |   |           broadcast.ts
    |   |           festival.ts
    |   |           education.ts
    |   |           index.ts
    |   |
    |   +---hooks
    |   +---types
    |   |       culture.ts
    |   |
    |   +---store
    |   \---pages
    |           Home.tsx
    |           Home.css
    |
    +---public
```

---

## 📅 [2026-03-08 14:20:00] [누적] 이원화 아키텍처 및 모듈화 사령부 설계 (Supreme V10.0)

### **🏗️ 1. 이원화된 세계 (World Separation Architecture)**
- **Service Domain (Web)**: `web/` 폴더. `localhost:5173`. 유저 전용.
- **Command Domain (Dashboard)**: `dashboard/` 폴더 (신규). `localhost:9999`. AI 지휘 전용.
- **API Bridge**: `data/agents/dashboard_api.py`. Port 5000 (Socket.IO). 모든 에이전트와 두 세계를 잇는 통로.

### **📂 2. 모듈화 사령부 UI (Dashboard Modular UI)**
- **상단: AI Status Hub (Cards)**
    - `MainCommandCard`: 메인 AI 전용 대형 카드. [실시간 작업 로그 + 명령 입력창].
    - `AgentStatusCards`: 4인 에이전트(Watcher, Guardian, Debater, Inspector) 소형 카드. [실시간 활동 상태 + 상태 점멸등].
- **하단: Unified Council Feed (Main)**
    - `CouncilFeed`: 6인(파트너+5 AI) 통합 실시간 토론창.

### **⚙️ 3. 파일 구조 모듈화 (Dashboard Component Tree)**
```text
dashboard/
└── src/
    ├── components/
    │   ├── Layout/           # 전체 그리드 레이아웃
    │   ├── Status/           # 에이전트 상태 카드 모듈
    │   ├── Command/          # 메인 AI 로그 및 명령 창 모듈
    │   └── Council/          # 6인 통합 토론 창 모듈
    ├── hooks/                # Socket.IO 통신 훅
    └── store/                # 전역 상태 관리
```

### **🛡️ 4. 가디언 지능형 고도화 (Guardian Intelligence Upgrade)**
- **이중 의도 확인 로직**: API 연결 끊김 대비, `live_chat.md`를 직접 파싱하여 파트너의 최신 의도를 2차 검증함.
- **작업 로그 웹 동기화**: 모든 차단 및 허용 로그를 `MainCommandCard`로 즉시 전송함.

---

## 📅 [2026-03-09 11:30:00] [누적] 사령부 통합 UI 혁신 및 실시간 신경망 (Supreme V11.0)

### **🏗️ 1. 아프리카TV 스타일 통합 UI (AfreecaTV Style Layout)**
- **목적**: 정보 밀도 극대화 및 사령관(파트너)의 지휘 편의성 확보.
- **구조**: `CouncilFeed.tsx`를 Grid 레이아웃(1fr 320px)으로 전면 개편.
    - **Left Section (80%)**: **Unified Chat Feed**. 대형 메시지 영역으로 가독성 확보.
    - **Right Top Section**: **Council Members**. 사령부 명단 및 실시간 접속/활동 상태 표시.
    - **Right Bottom Section**: **Command Input**. 전용 명령 입력창(Textarea) 배치. `Enter` 키 전송 지원.

### **📡 2. 실시간 신경망 브릿지 (Neural Bridge Architecture)**
- **CLI <-> Dashboard 양방향 동기화**: 터미널의 메인 AI와 웹 대시보드를 물리적으로 연결.
- **REST API 확장**: `dashboard_api.py`에 고속 통신 엔드포인트 추가.
    - `/api/log` (POST): CLI의 작업 내역을 대시보드 'MAIN AI COMMANDER' 로그로 실시간 전송.
    - `/api/chat` (POST): CLI의 답변 내용을 대시보드 토론창으로 실시간 전송.
- **Persistence Layer**: 모든 대화는 `live_chat.md`에 Literal하게 기록되며, 페이지 새로고침 시 초기 히스토리 동기화에 사용됨.

3. **데이터 동기화 메커니즘 (Sync Mechanism)**
1. **Dashboard Input** -> **Socket.IO** -> **Backend** -> **live_chat.md (Write)**.
2. **Main AI (CLI)** -> **Read live_chat.md** (Check for new commands) -> **Execute Task**.
3. **Main AI (CLI)** -> **REST API (/api/log, /api/chat)** -> **Dashboard (UI Update)**.

---

## 📅 [2026-03-09 14:00:00] [누적] 사령부 통합 아키텍처 및 메인 AI 리포터 (Supreme V12.0)

### **🏗️ 1. 통합 에이전트 서버 (Monolithic Agent Server)**
- **개념**: 개별 실행되던 4인 에이전트(Watcher, Guardian, Debater, Inspector) 소켓 클라이언트를 제거하고, `dashboard_api.py` 내부 로직으로 완전 통합.
- **장점**: 윈도우 환경에서의 소켓 불안정성 해소, 프로세스 관리 간소화, 응답 속도 최적화.
- **워크플로우**:
    1. **입력**: 파트너가 대시보드 또는 CLI를 통해 메시지 전송.
    2. **트리거**: `dashboard_api.py`가 메시지를 수신하는 즉시 내부 `AgentManager` 가동.
    3. **추론**: `roles.json`(페르소나)과 `rules.json`(수칙), `IDENTITY.md`(헌법)를 기반으로 Gemini API가 4인 에이전트의 응답을 생성.
    4. **출력**: 생성된 응답들을 대시보드로 즉시 브로드캐스트하고 `live_chat.md`에 기록.

### **📡 2. 메인 AI 리포터 (Main AI Reporter)**
- **기능**: 터미널(CLI)에서 활동하는 메인 AI(나)의 내부 사고 과정과 작업 로그를 대시보드 상단 명령창에 실시간 동기화.
- **구현**: `dashboard_api.py`에 전용 엔드포인트 `/api/main_ai_report` 구축. CLI가 작업을 수행할 때마다 해당 API를 호출하여 상태를 보고.

### **📂 3. 이식성 강화 (Portability & Path Normalization)**
- **원칙**: 모든 드라이브 문자(D:/, H:/ 등) 기반 절대 경로를 완전 박멸.
- **구현**: `os.path.dirname(__file__)`을 기준으로 상대 경로를 계산하여 USB나 다른 PC로 이동해도 즉시 작동하는 환경 구축.

### **⚙️ 4. 에이전트 정리 (Agent Cleanup)**
- **대상**: 통합 완료 후 불필요해진 `watcher.py`, `guardian.py`, `debater.py`, `inspector.py`, `base_agent.py`는 레거시 폴더로 이동하거나 삭제하여 `data/agents/` 폴더의 순도를 유지.

---

## 📅 [2026-03-10 16:30:00] [누적] V14.4: The Great Departure (수평적 소켓 사령부 및 프로세스 독립화)

### **🏗️ 1. 수평적 소켓 사령부 (Horizontal Socket Architecture)**
- **도입 배경**: 터미널의 `input()` 블로킹 구조를 탈피하고, 파일 기반 소통(`live_chat.md`, `task.txt` 등)의 지연 및 충돌 문제를 근본적으로 해결.
- **핵심 철학**: "너를 통하지 마라 (Do not go through me)". 메인 AI를 포함한 모든 에이전트가 허브에 평등하게 접속하여 상호 감시 및 토론 수행.

### **📡 2. 순수 신경 허브 (Pure Nerve Hub - dashboard_api.py)**
- **역할**: 메시지를 기록하거나 직접 판정하지 않는 '순수 중계소'로 리팩토링.
- **기능**:
    - **Active Agent Inventory**: 소켓에 물리적으로 접속된 에이전트 목록을 실시간 관리(`active_agents`).
    - **Status Broadcasting**: 에이전트의 접속/해제 상태를 `agent_status_update` 이벤트를 통해 대시보드에 즉각 전파.
    - **Dynamic Port Discovery**: 5055번부터 빈 포트를 자동 탐색하여 `data/port.txt`에 기록, USB 이동성 확보.

### **🤖 3. 독립 멀티 프로세스 에이전트 (Independent Multi-Process Agents)**
- **구조**: `watcher_v14.py`, `guardian_v14.py` 등 5개 에이전트가 각자의 **`.venv` 가상환경 파이썬 프로세스**로 독립 실행.
- **지능**: `BaseAgentV14`를 상속받아 `GeminiEngine`을 탑재. 8대 필수 문서를 시작 시 뇌에 박아넣는 '지능 각인(Imprinting)' 수행.
- **채널 인식형 응답**:
    - **Council Channel**: 전략 토론 메시지 수신 시 토론창(`speak`)으로 응답.
    - **Log Channel**: 커맨드 센터 명령 수신 시 로그창(`send_log`)으로 판정 결과 전송.

### **💻 4. 데이터 실재론 기반 UI (Data Realism Dashboard)**
- **기능**: LED 상태 및 참가자 목록을 '가짜 하트비트'가 아닌 '실제 소켓 세션'과 1:1 동기화.
- **Empty Seat System**: 연결되지 않은 에이전트는 이름을 숨기고 `--- EMPTY ---`로 표시하여 시각적 혼선 제거.
- **Main AI Command Card**: 메인 AI 전용 LED 및 `SYSTEM INITIALIZING` 상태창 도입으로 생사여부 투명화.

### **⚙️ 5. 파일 구조 및 실행 스택 (System Execution Stack)**
- **Bootstrapper**: `g.ps1` (USB-Ready). 허브 가동 -> 포트 감지 -> 5인 에이전트 병렬 가동 -> 대시보드 가동.
- **Environment**: `.vscode/settings.json`을 통한 터미널 자동 활성화 및 중복 입력 방지 최적화.

### **🛠️ 6. GitHub MCP 통합 (External Tool Expansion)**
- **목적**: 사령부 에이전트들이 직접 깃허브 레포지토리를 관리, 이슈 생성, 코드 리뷰를 수행할 수 있는 외부 도구 권한 부여.
- **설정**: `.gemini/settings.json` (Project Scope)에 `npx @modelcontextprotocol/server-github` 추가.
- **보안**: 하드코딩된 토큰 대신 환경 변수(`$GITHUB_PERSONAL_ACCESS_TOKEN`) 참조 방식을 사용하여 `.env` 파일과 통합 관리.
- **활용**: 메인 AI 및 에이전트들이 필요 시 깃허브 API를 호출하여 협업 데이터 동기화 및 코드 버전 관리 보조.


### 🛡️ 셧다운 및 자원 관리 (Resource Management)
- **Zero-Interference Surgical Shutdown**: 프로젝트와 관련된 프로세스만 정밀 타격하여 시스템의 다른 작업에 영향을 주지 않으면서 클린업 수행.
- **이중 안전장치 (Dual Safety)**:
    1. **Active Kill**: 부팅(g.ps1) 및 종료(dashboard_api.py) 시 PowerShell을 통한 경로 기반 프로세스 강제 종료.
    2. **Passive Suicide (Self-Termination)**: 에이전트(BaseAgentV14.py)가 허브 연결 상실 시 5초 후 스스로 종료하여 유령 프로세스화 방지.
- **PID Protection**: 현재 제어권을 가진 CLI 및 파워쉘 세션의 PID를 화이트리스트로 관리하여 작업 연속성 보장.

---

## 🏛️ [2026-03-11] [누적] 최신 통합 아키텍처 명세 (Supreme V15.7 Standard)

### **🛠️ 1. 최신 기술 스택 (Latest Tech Stack)**
| 구분 | 기술 / 라이브러리 | 상세 비고 |
| :--- | :--- | :--- |
| **언어 (Core)** | Python 3.14+ / TypeScript 5.0+ | 비동기(Async) 처리 및 타입 안정성 극대화 |
| **허브 (Hub)** | **FastAPI + Uvicorn (ASGI)** | 기존 Flask에서 현대적 ASGI 서버로 전면 마이그레이션 |
| **통신 (Nerve)** | **Async Socket.IO** | 에이전트-허브 간 실시간 양방향 신경망 구축 |
| **엔진 (AI)** | **Google GenAI SDK (Gemini 2.0 Flash)** | 지수 백오프(Backoff) 및 429 쿼터 방어 로직 탑재 |
| **웹 (World 1/2)** | React (Vite) + Vanilla CSS | '웹 우선' 접근 및 고성능 번들링 (Port 5173 / 9999) |
| **인프라 (Infra)** | PowerShell V7 + .venv | 포터블 가상환경 및 정밀 타격(Surgical Kill) 셧다운 체계 |

### **📂 2. 정예 프로젝트 폴더 구조 (Project Anatomy)**
`	ext
D:\PROJECT-ALL\my-project\
├───g.bat / g.ps1               # 통합 기동 및 좀비 프로세스 박멸 스크립트
├───AI_CORE\                    # 사령부 핵심 헌법 및 매뉴얼
│   ├───IDENTITY.md             # 프로젝트의 존재 이유 및 핵심 가치
│   └───HANDOVER.md             # 세션 간 인수인계 데이터
├───프로젝트_기록\              # 6대 필수 기록 문서 (1~6번)
├───dashboard\                  # [World 2] 사령부 전략 대시보드 (Port 9999)
│   └───src\components\         # Council Feed, Command Card 등 전략 UI
├───web\                        # [World 1] FREEISM 메인 서비스 (Port 5173)
│   └───src\                    # Midnight Prism 테마 기반 브랜드 웹사이트
├───data\
│   ├───port.txt                # 유동적 포트 동기화 파일
│   ├───boot.ps1                # 마스터 허브 부트스트랩
│   ├───agents\                 # 5인 독립 에이전트 군단 (V14.7)
│   │   ├───base_agent_v14.py   # 에이전트 공통 로직 (자폭/보호 탑재)
│   │   ├───dashboard_api.py    # ASGI 사령부 허브 (Port 5055)
│   │   ├───main_ai_v14.py      # 사령관 (독립 데몬)
│   │   ├───watcher_v14.py      # 감시자 (실시간 모니터링)
│   │   ├───guardian_v14.py     # 수호자 (데이터 무결성)
│   │   ├───inspector_v14.py    # 감사관 (로직 검증)
│   │   ├───debater_v14.py      # 토론가 (비판적 사고)
│   │   └───engines\            # AI 엔진 추상화 레이어
│   └───logs\                   # 정밀 타격 로그 및 신경망 기록
└───.venv\                      # 독립 가상환경 (Portable)
`

### **📡 3. 신경망 통신 및 보안 아키텍처 (Neural & Security)**
1.  **Independent Daemonization**: 모든 에이전트는 개별 프로세스로 구동되어 input() 블로킹 없이 자율적으로 사고하고 보고함.
2.  **Neural Bridge (ASGI)**: FastAPI 허브가 모든 에이전트의 소켓 패킷을 수집하여 대시보드와 터미널에 실시간으로 브로드캐스트.
3.  **Surgical Shutdown V3**:
    - **PID Protection**: 현재 세션(8868) 보호 로직을 통한 작업 연속성 확보.
    - **Self-Termination**: 허브 상실 시 에이전트의 자발적 종료로 좀비 프로세스 발생 원천 차단.
4.  **Brand Identity Convergence**: FREEISM 브랜드 철학(프리즘, 스포트라이트)을 시스템 설계 전반(이원화된 사령부 구조)에 투영.

### **🎨 4. 디자인 시스템 (Midnight Prism Standard)**
- **Global Theme**: #111329 (Midnight Prism Indigo)
- **Point Theme**: #f0f2ff (Refined White Spotlight)
- **Symbolism**: **FREE** (800 Heavy) + **ISM** (200 Light) / 굴절과 연결의 Prism 메타포 적용.

---

## 🏛️ [2026-03-11] [누적] V17.5 공정 중심 감시 체계 확립 (Standard Blueprint)

### **🛡️ 1. 4단계 정예 공정 감시자 (The 4-Step Process Guard)**
| 공정 단계 | 에이전트 명칭 | 핵심 임무 | 통제 권한 |
| :--- | :--- | :--- | :--- |
| **작업 전** | **Planner** | 계획의 타당성 및 브랜드 가치(8대 문서) 검증 | **작업 승인/반려권** |
| **작업 중** | **Flow Watcher** | 실시간 맥락(Context) 유지 및 디자인 규격 감시 | **경고 발령/인터럽트** |
| **작업 중** | **Safety Guard** | 기술적 사고(오타, 유실, 좀비 프로세스) 실시간 차단 | **프로세스 즉시 사살(Sigkill)** |
| **작업 후** | **Inspector** | 계획 대비 결과물 일치 여부 및 마스터피스 QA | **최종 완료 보고 승인권** |

### **📡 2. 인프라 운영 원칙 (Operations)**
- **Multi-API Isolation**: 5개 API 키를 1:1로 할당하여 에러 없는 실시간 감시 보장.
- **Blueprint-First Implementation**: 모든 구현은 AI_CORE/HANDOVER.md의 명세를 100% 준수한다.
- **6인 합동 토론**: 시스템 구축 후, 사령관(나), 4인의 감시자, 그리고 파트너님이 모여 실제 데이터를 바탕으로 '공연 마스터피스'를 완성한다.

---

## 🏛️ [2026-03-11 15:50:00] [누적] V17.5 4단계 공정 감시 체계 (Standardized Process Control)

### **🛠️ 1. 사령부 정예화 (Command Elite Re-Architecture)**
- **정의**: 기존의 자율 토론 체계를 넘어, 작업의 [전-중-후] 전 과정을 물리적으로 제어하는 **'4단계 공정 감시 시스템'**을 가동함.
- **핵심 구성 (The Elite 5)**:
    1.  **Main AI (Commander)**: `main_ai_v14.py` (Key 1). 총괄 지휘 및 실행.
    2.  **Planner (Pre-work)**: `planner_v15.py` (Key 2). 계획 타당성 및 브랜드 정렬 검증 (작업 승인권).
    3.  **Flow Watcher (In-build)**: `watcher_v15.py` (Key 3). 실시간 컨텍스트 이탈 및 디자인 규격 감시 (경고권).
    4.  **Safety Guard (In-build)**: `safety_guard_v15.py` (Key 4). 오타, 생략, 파괴적 행위 실시간 차단 (Sigkill권).
    5.  **Inspector (Post-work)**: `inspector_v15.py` (Key 5). 계획 대비 결과물 1:1 QA 및 최종 보고 승인 (완료 보고 승인권).

### **📡 2. API 격리 및 쿼터 최적화 (API Isolation)**
- **1:1 매핑**: 5명의 요원이 각각 독립된 API 키를 사용하여 실시간 병렬 감시 중 발생하는 429(Quota Exhausted) 에러를 원천 차단.
- **지능 각인(Intelligence Imprinting)**: 기동 시 8대 필수 문서를 엔진 세션에 주입하여 추론 일관성 확보.

### **🛡️ 3. 물리적 강제력 (Physical Enforcement)**
- **Sigkill 메커니즘**: `Safety Guard`가 위험 요소를 감지하면 `nerve_kill_signal`을 허브에 전송, 메인 AI의 작업 프로세스를 즉시 중단시켜 사고 방지.
- **Veto 시스템**: `Planner`와 `Inspector`의 `ALLOW` 신호 없이는 작업의 시작과 끝이 불가능한 '엄격한 승인 루프' 형성.

### **📂 4. 최신 파일 구조 (Current File Stack)**
```text
data/agents/
├── base_agent_v14.py   # 상속용 베이스 (API 키 맵핑 및 자폭 로직)
├── main_ai_v14.py      # 사령관
├── planner_v15.py      # 계획 설계자
├── watcher_v15.py      # 흐름 감시자
├── safety_guard_v15.py # 기술 보안관 (V17.5 신규)
└── inspector_v15.py    # 감사관 (V17.5 신규)
```

---

## 🏛️ [2026-03-11 18:45:00] [누적] V18.0 이원화 사령부 물리적 인프라 설계

### **1. 이원화 터미널 실행 구조 (Dual-Terminal Execution)**
- **Terminal 1 (Executor)**: `gemini cli` 메인 세션. 파일 시스템 조작 권한(Tool Use)을 가진 유일한 실행 주체.
- **Terminal 2 (Liaison)**: `gemini cli` 독립 세션. `development_log.md` 실시간 모니터링 및 대시보드 토론(Council) 지휘.
- **연결 매개**: `data/logs/development_log.md` (작업 데이터) + `Socket Hub` (실시간 신호).

### **2. 실시간 지능 동기화 신경망 (Dynamic Re-Imprinting)**
- **구조**: `dashboard_api.py` -> `re_imprint` 브로드캐스트 -> 각 에이전트의 `GeminiEngine` 세션 리셋.
- **메커니즘**:
    1. 사령관(T1)이 8대 문서를 수정함.
    2. 수정 완료 후 `re_imprint` 신호를 허브에 발송.
    3. 모든 에이전트(Liaison 포함)가 즉시 문서를 다시 읽고 뇌(Memory)를 최신화함.

### **3. 무소음 실시간 감시 (Zero-Quota Monitoring)**
- **Liaison Console**: API 호출 없이 모든 소켓 메시지를 실시간으로 콘솔에 출력하여 파트너에게 투명한 감시 환경 제공.

---
