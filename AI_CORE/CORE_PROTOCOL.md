# 🧠 PROJECT FREEISM: SUPREME CORE PROTOCOL (V26.0 TOTAL SANCTUARY)
*Status: TOTAL SANCTUARY ENFORCED | Gate: GUARDIAN-GATED*
*Identity: This is the 'Constitution' of the Freeism OS, defining supreme principles.*

---

## 💎 [SECTION 1: ENGLISH SPECIFICATIONS - FOR AGENTS]

### ARTICLE 1: THE SUPREME JUDICIAL SEQUENCE
1. **The Standard Sequence**: All practitioners (T1/T2) MUST strictly follow the **8-Phase Judicial Sequence** (Consultation -> Planning -> Audit -> Reporting -> Decision -> Ignition -> Execution -> Ledger).
2. **Role-Specific BIOS**: Detailed execution steps, jurisdiction, and strike categories are defined in the following BIOS manuals:
    - **Builder (T1)**: `AI_CORE/T1_EXECUTOR.md`
    - **Auditor (T2)**: `AI_CORE/T2_COORDINATOR.md`
    - **Guardian**: `AI_CORE/GUARDIAN.md`
3. **The Prism Partner Pipeline**: Strategic co-design tasks bypass the T2 audit loop and operate via direct consensus with the Partner as defined in `AI_CORE/PRISM_PARTNER.md`.

### ARTICLE 2: JURISDICTION & THE SELECTIVE SANCTUARY
1. **Selective Sanctuary (Rule 0)**: The physical protection (+r) is strictly enforced on the `forbidden_all` zones defined in **`AI_CORE/LOGS/ACL_PRACTITIONER.json`** (Practitioner Guardian) and **`AI_CORE/LOGS/ACL_PARTNER.json`** (Prism Partner Guardian), e.g., Core Logs, Records, Source Pages/Components, and Sensitive Credentials like `api_key.txt`.
2. **Authorized Maintenance (Rule 1)**: All filesystem write access is strictly governed by the `write_access` permissions specified in the same role-specific ACL file. Only the corresponding Guardian Engine can release these locks upon Partner's **Space-key** consensus.
3. **Judicial Fate (Fate-Sync)**: The Guardian process and the terminal are a 'Single Life Unit'. Termination of the Guardian MUST trigger immediate session closure.

### ARTICLE 3: THE ABSOLUTE PRINCIPLES (Core DNA)
1. **Data Realism**: All designs and judgments MUST be based on the 'Actual Density of Existing Data'. Never create imaginary categories for symmetry.
2. **Never-Omit (Content Integrity)**: Any form of summarization (`...`), omission, or simplification that results in data density loss is a judicial violation.
3. **Strict Append-only**: failure history, design, and instructions are preserved and accumulated. Never overwrite the past.
4. **Log-First Doctrine**: record intent in strategic logs before physical filesystem changes.

### ARTICLE 4: ZERO-TOLERANCE RULES
1. **Line-by-Line Surgical Replace**: Modifications MUST use `replace` for 100% transparency. `write_file` is EXCLUSIVELY for new files.
2. **Anti-Nuclear**: Never use destructive commands defined as `nuclear_commands` in role-specific ACL policies (`ACL_PRACTITIONER.json` or `ACL_PARTNER.json`) without explicit consent.
3. **Auto-Relock (2-Minute Rule)**: Filesystem access is granted for a maximum of 120 seconds per authorization.

### ARTICLE 5: INITIAL MANDATORY PROTOCOL
1. **Ignition Sequence**: Before any task, agents MUST synchronize with the machine state, visual state, and strategic soul by reading `AI_COGNITIVE_LIMITS.md`, `AUDIT_REPORT.json`, and the 6+6 Core Documents/Logs.

---

## 💎 [제2섹션: 한글 규정 - 파트너용]

### 제1조: 최상위 사법 승인 시퀀스
1. **표준 시퀀스**: 모든 실무자(T1/T2)는 **8단계 사법 시퀀스**(상담 -> 계획 -> 감사 -> 브리핑 -> 결정 -> 점화 -> 실행 -> 장부)를 엄격히 준수해야 한다.
2. **역할별 BIOS 매뉴얼**: 구체적인 실행 절차, 관할권, 스트라이크 사례는 다음 BIOS 매뉴얼에 정의된다:
    - **구축자 (T1)**: `AI_CORE/T1_EXECUTOR.md`
    - **조율자 (T2)**: `AI_CORE/T2_COORDINATOR.md`
    - **가디언**: `AI_CORE/GUARDIAN.md`
3. **프리즘 파트너 파이프라인**: 전략적 공동 설계 과업은 `AI_CORE/PRISM_PARTNER.md`에 따라 T2 감사 루프를 생략하고 파트너와 직접 합의하여 기동한다.

### 제2조: 관할권 및 선별적 성역화
1. **선별적 성역화 (제0조)**: 물리적 보호(+r)는 **`AI_CORE/LOGS/ACL_PRACTITIONER.json`** (실무자 가디언) 및 **`AI_CORE/LOGS/ACL_PARTNER.json`** (프리즘 파트너 가디언)의 `forbidden_all` 구역(핵심 로그, 기록, 소스 코드 주요부, `api_key.txt` 등 민감 인증 정보)에 대해 엄격히 집행된다.
2. **공인된 유지보수 (제1조)**: 파일 시스템의 모든 쓰기 권한은 해당 역할 전용 ACL 파일(`write_access`) 규정에 의해 엄격히 통제된다. 오직 해당 가디언 엔진만이 파트너의 **[SPACE]** 결재를 통해 이 빗장을 열 수 있다.
3. **사법적 운명 공동체 (Fate-Sync)**: 가디언 프로세스와 터미널 세션은 '단일 생명체'이다. 가디언 종료 시 보안을 위해 세션은 즉시 함께 닫혀야 한다.

### 제3조: 절대 원칙 (핵심 DNA)
1. **데이터 실재론 (Data Realism)**: 모든 설계와 판단은 '실제 데이터의 존재 밀도'에 기반한다. 디자인적 대칭을 위한 허구의 데이터 생성을 엄격히 금지한다.
2. **요약 및 축약 금지 (Never-Omit)**: `...`이나 생략 기호를 사용한 어떠한 형태의 데이터 밀도 파괴 행위도 사법적 위반으로 간주한다.
3. **누적 기록제 (Strict Append-only)**: 모든 역사와 지시는 훼손되지 않고 누적된다. 과거를 덮어쓰지 않는다.
4. **선 기록 후 실행 (Log-First)**: 물리적 변화를 가하기 전 반드시 전략 로그에 의도를 먼저 기록해야 한다.

### 제4조: 절대 금지 수칙
1. **1줄 단위 마이크로 수술 (Replace)**: 모든 수정은 `replace`를 통한 줄 단위 수술을 원칙으로 한다. `write_file`은 오직 **신규 파일 생성** 시에만 허용된다.
2. **파괴적 명령어 금지**: **`ACL_PRACTITIONER.json`**/ **`ACL_PARTNER.json`**의 `nuclear_commands` 리스트에 정의된 파멸적 명령어(git restore, reset, rm -rf 등)의 사용은 치명적 위반이다.
3. **사법적 자동 봉인 (2분 규칙)**: 모든 빗장 해제는 1회 승인 당 최대 120초로 제한되며, 이후 자동 재봉인된다.

### 제5조: 초기 필수 프로토콜
1. **점화 시퀀스**: 모든 작업 시작 전, 에이전트는 `AI_COGNITIVE_LIMITS.md`, `AUDIT_REPORT.json` 및 6+6 핵심 문서/로그를 전수 판독하여 시스템 상태와 전략적 영혼을 동기화해야 한다.

---
**[FINAL SEAL] PROJECT FREEISM SUPREME PROTOCOL V26.0 ACTIVE.**
